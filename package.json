{
  "devDependencies": {
    "serverless-localstack": "^1.2.0"
  },
  "scripts": {
    "build": "sls webpack --stage local",
    "deploy": "sls deploy --stage local",
    "watch": "nodemon -w src -e '.*' -x 'npm run build'",
    "dev": "sls dev --stage local"
  }
}
